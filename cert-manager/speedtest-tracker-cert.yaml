apiVersion: cert-manager.io/v1
kind: Certificate
metadata:
  name: speedtest-tracker-cert
  namespace: speedtest-tracker
spec:
  secretName: speedtest-tracker-tls
  dnsNames:
  - speedtest-tracker.stone-lab.local
  issuerRef:
    name: internal-ca
    kind: ClusterIssuer
