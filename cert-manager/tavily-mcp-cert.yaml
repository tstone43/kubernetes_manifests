apiVersion: cert-manager.io/v1
kind: Certificate
metadata:
  name: tavily-mcp-cert
  namespace: llama-cpp
spec:
  secretName: tavily-mcp-tls
  dnsNames:
  - tavily-mcp.stone-lab.local
  issuerRef:
    name: internal-ca
    kind: ClusterIssuer
