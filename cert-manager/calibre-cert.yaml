apiVersion: cert-manager.io/v1
kind: Certificate
metadata:
  name: calibre
  namespace: calibre
spec:
  secretName: calibre-tls
  dnsNames:
  - books.stone-lab.local
  issuerRef:
    name: internal-ca
    kind: ClusterIssuer